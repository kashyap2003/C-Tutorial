#include <stdio.h>

int main(void)
{
    /*
    Multiplicative and additive operators
    + (addition)
    - (substraction)
    * (Multiplication)
    / (division)
    % (Modulus)
    */

   float a=11 , b=2;
   float divid;
   divid = a / b;
   printf("Division = %f", divid);
}