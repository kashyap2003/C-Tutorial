#include <stdio.h>

int main(void)
{
    /*
    Multiplicative and additive operators
    + (addition)
    - (substraction)
    * (Multiplication)
    / (division)
    % (Modulus)  ---> Reminder gives.
    */

   int a=11 , b=2;
   int reminder;
   reminder = a % b;
   printf("Modulus = %d", reminder);
}