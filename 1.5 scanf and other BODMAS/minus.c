#include <stdio.h>

int main(void)
{
    /*
    Multiplicative and additive operators
    + (addition)
    - (substraction)
    * (Multiplication)
    / (division)
    % (Modulus)
    */

   int a=11 , b=2;
   int minus;
   minus = a - b;
   printf("Minus = %d", minus);
}